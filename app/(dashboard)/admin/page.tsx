import React from 'react'

export default function AdminDashboard() {

  
  // Error handling in Server Components can be done by throwing an error. This will trigger the nearest error.js file in the route hierarchy.
  // throw new Error("Admin Dashboard Error")  

  return (
    <div>Admin Dashboard</div>
  )
}
